# Тренировка Памяти - Веб-приложение на C++

Современное веб-приложение для тренировки памяти, написанное на C++17 с использованием чистых принципов архитектуры.

## Особенности

- 🎮 **Два типа игр**: Последовательность и Пары
- 📊 **Три уровня сложности**: Легкий, Средний, Сложный
- 🎨 **Современный UI**: Красивый и интуитивный интерфейс
- ⚡ **Высокая производительность**: Нативный C++ сервер
- 🏗️ **Чистая архитектура**: Разделение на слои (модели, сервисы, контроллеры)

## Архитектура проекта

```
ci_project/
├── CMakeLists.txt          # Конфигурация сборки
├── include/                # Заголовочные файлы
│   ├── memory_game.h       # Модели игр
│   ├── memory_service.h    # Сервисный слой
│   └── api_controller.h    # API контроллер
├── src/                    # Исходный код
│   ├── main.cpp           # Точка входа и HTTP сервер
│   ├── memory_game.cpp    # Реализация игр
│   ├── memory_service.cpp # Реализация сервиса
│   └── api_controller.cpp # Реализация API
└── web/                    # Фронтенд
    ├── index.html         # HTML страница
    ├── style.css          # Стили
    └── app.js             # JavaScript логика
```

## Принципы архитектуры

### 1. Разделение ответственности (Separation of Concerns)
- **Модели** (`memory_game.h/cpp`): Бизнес-логика игр
- **Сервисы** (`memory_service.h/cpp`): Управление жизненным циклом игр
- **Контроллеры** (`api_controller.h/cpp`): Обработка HTTP запросов
- **Сервер** (`main.cpp`): Сетевая логика

### 2. Современный C++
- Использование `std::shared_ptr` для управления памятью
- RAII принципы
- STL контейнеры (`std::vector`, `std::unordered_map`)
- Многопоточность (`std::thread`, `std::mutex`)
- Namespace для организации кода

### 3. Инкапсуляция
- Приватные методы в контроллерах
- Дружественные классы для доступа к приватным методам
- Защита данных через мьютексы

## Сборка и запуск

### Требования
- C++17 компилятор (GCC 7+, Clang 5+)
- CMake 3.15+
- Linux (для сетевых сокетов)

### Сборка

```bash
mkdir build
cd build
cmake ..
make
```

### Запуск

```bash
cd build/bin
./MemoryTrainer
```

Сервер запустится на порту 8080. Откройте браузер и перейдите по адресу:
```
http://localhost:8080
```

## API Endpoints

### POST /api/game
Создание новой игры
- Query параметры:
  - `type`: `sequence` или `pairs`
  - `difficulty`: `easy`, `medium` или `hard`

### GET /api/game/{gameId}
Получение информации об игре

### POST /api/game/{gameId}/check
Проверка ответа
- Body: `{"answer": [1, 2, 3, 4]}`

### DELETE /api/game/{gameId}
Удаление игры

## Как играть

1. Выберите тип игры (Последовательность или Пары)
2. Выберите уровень сложности
3. Нажмите "Начать игру"
4. Запомните последовательность чисел
5. После истечения времени введите последовательность
6. Нажмите "Проверить" для проверки ответа

## Разработка

### Добавление нового типа игры

1. Создайте новый класс, наследующийся от `MemoryGame`
2. Реализуйте виртуальные методы
3. Добавьте тип в `GameType` enum
4. Обновите `MemoryService::createGame()`

### Улучшения для продакшена

- Использование полноценного HTTP фреймворка (Crow, Drogon)
- Добавление базы данных для сохранения результатов
- Аутентификация пользователей
- HTTPS поддержка
- Логирование
- Unit тесты

## Лицензия

MIT License


